<%= link_to spree.cart_path,  class: "cart-icon cart-icon--visible-count #{local_assigns[:class]} cart-icon",
  'aria-label': Spree.t(:cart) do %>
  <%= icon(name: 'cart_new', width: 30, height: 24) %>

  <span class="font-weight-medium cart-icon-count"><%= item_count %></span>
<% end %>